//This is a dummy file to start off work on the function that accepts a reasonable range as having "reached" the target.
//Declare any variables that need to be shared within this block  
float myPosition[12];
float targetPosition[12];

Boolean hasReachedTargetPosition(){
	//calculate if we are close enough to the spot we want to be
	if (are we close enough to target){
		DEBUG(("myPosition %f",myPosition[count]));
		DEBUG(("targetPosition %f",targetPosition[count]));
	}
	
}
