//This is a dummy file to start off work on the function that monitors the distance between two or more objects to avoid collisions.
//Declare any variables that need to be shared within this block  
float myPosition[12];
float otherObjectPosition[12];


void avoidCollision(){
	//This function is called once per second.  Use it to control the satellite.
	if (myPosition is too close to otherObjectPosition || my Vector will intercept otherObjects vector){ // fix for these ideas
		DEBUG(("myPosition %f",myPosition[1])); 
		DEBUG(("otherPosition %f",otherObjectPosition[1]));
	}
}
